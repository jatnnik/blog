---
import { formatDate, type DateContext } from '@/utils/date'

interface Props {
  date: Date
  context: DateContext
}

const { date, context } = Astro.props
---

<time datetime={date.toISOString()}>
  <Fragment set:html={formatDate(date, context)} />
</time>
